{
  "name": "election-management-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/api",
    "packages/web",
    "packages/shared"
  ],
  "scripts": {
    "dev:api": "cd packages/api && npm run dev",
    "dev:web": "cd packages/web && npm run dev",
    "dev": "concurrently -n api,web,green,magenta \"npm run dev:api\" \"npm run dev:web\"",
    "build:api": "cd packages/api && npm run build",
    "build:web": "cd packages/web && npm run build",
    "seed": "cd packages/api && npm run seed",
    "setup": "npm install && cd packages/mobile && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.0"
  },
  "//overrides": "Pin a single React across workspaces — the RN mobile pkg hoists react@18.2.0 to root while web nested 18.3.1, which broke styled-jsx (dual-React useContext crash) during `next build` prerender of /404,/500.",
  "overrides": {
    "react": "18.2.0",
    "react-dom": "18.2.0"
  }
}
